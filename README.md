# robot_patrol

Small ROS2 demo app that cycles through waypoints and publishes patrol status.

## Layout

- `ros2_ws/src/robot_patrol`: ROS2 Python package

## Quickstart

```bash
cd ros2_ws
colcon build
source install/setup.bash
ros2 run robot_patrol patrol_node
```
